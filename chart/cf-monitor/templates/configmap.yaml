apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cf-monitor.fullname" . }}
  labels:
    {{- include "cf-monitor.labels" . | nindent 4 }}
data:
  {{- if .Values.config }}
  healthcheckconfig.json: |-
  {{ toJson .Values.config | indent 4 }}
  {{- end }}
